DarkGrid editable exact-look site

This version uses editable HTML and CSS sections instead of one full-page image.

Upload to GitHub:
- replace index.html
- replace style.css
- keep your darkgrid-logo.jpg in the repo root
- commit changes

After upload, Vercel will redeploy automatically.
